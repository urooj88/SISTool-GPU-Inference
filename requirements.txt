# Core data processing
pandas>=1.5.0
openpyxl>=3.1.0

# Terminal output
rich>=13.0.0

# Dataset loading (HuggingFace)
datasets>=2.0.0

# System monitoring
psutil>=5.9.0

# Standard library only — no install needed:
# os, sys, subprocess, csv, re, random, argparse
# pathlib, copy, threading, time
